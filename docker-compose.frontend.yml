services:
  go-app:
    image: ghcr.io/vicmanbrile/go-backend:latest
    ports:
      - "8000:8000"
    environment:
      - MONGO_URI=mongodb://mongodb:27017/finanzas
    depends_on:
      - mongodb
    networks:
      - dev-network

  mongodb:
    image: mongo:4.4.18
    ports:
      - "27017:27017"
    volumes:
      - mongo-dev-data:/data/db
    networks:
      - dev-network

volumes:
  mongo-dev-data:

networks:
  dev-network:
    driver: bridge